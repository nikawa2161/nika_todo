%h1
  New Board
= form_with(model: @borad, url: borads_path, method: 'post', local: true) do |f|
  %div
  = f.label :title, 'name'
  %div
    = f.text_field :title, class: 'text'
  %div
    = f.label :content, 'Description'
  %div
    = f.text_area :content
  = f.submit 'Submit', class: 'btn-primary'
